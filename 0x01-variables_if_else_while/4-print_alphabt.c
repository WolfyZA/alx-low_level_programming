#include <stdio.h>
/**
  *main - entry point
  *Description: 'alphabet in lowercase'
  *Return: always 0
  */
int main(void)
{
	int a = 97;

	while (n <= 122)
	{
		if (n == 101 || n == 113)
		{
			n++
			continue;
		}
		putchar(a);
		n++
	}
	putchar('\n');
	return (0);
}
