#include <stdio.h>
/**
  *main - entry point
  *Description: 'print numbers from 0 to 10'
  *Return: always 0
  */
int main(void)
{
	int num;

	for (num = 0; num <= 9; num++)
	{
		putchar(n + 48);
	}
	putchar('\n');
	return (0);
}
